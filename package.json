{
  "name": "mat-bang-9x30",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "check": "npm run build && npm run spec:check && npm run check:grid && npm run check:3d",
    "spec": "node scripts/gen-spec.mjs",
    "spec:check": "node scripts/gen-spec.mjs --check",
    "check:grid": "node scripts/check-grid.mjs",
    "check:3d": "node scripts/check-3d.mjs"
  },
  "dependencies": {
    "next": "^16.3.4",
    "react": "^19.3.0",
    "react-dom": "^19.3.0",
    "three": "^0.186.0"
  }
}
